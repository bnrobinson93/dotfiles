{
  // Extend the Quickshell Omarchy menu with JSONC.
  //
  // IDs are object keys. The parent is inferred from the dotted id, so
  // "personal.notes" appears under "personal", and "personal" appears on the
  // root menu. Reuse an existing id to override/extend it.
  //
  // Fields:
  //   icon        Nerd Font glyph shown in the icon column.
  //   label       Visible row title.
  //   action      Shell command to run. If omitted, the row is a submenu.
  //   target      Existing submenu id to open. Use for links/aliases.
  //   provider    Runtime provider function/command returning JSON rows.
  //   aliases     alternate `omarchy menu summon <name>` routes; also searchable.
  //   description Optional subtitle and extra search text.
  //   when        Shell condition; hide row when it fails.
  //   checked     Shell condition; append ✓ when it succeeds.
  //
  // Examples:
  // "personal": {"icon":"","label":"Personal"},
  // "personal.notes": {"icon":"󰎞","label":"Notes","action":"omarchy-launch-editor ~/notes"},
  // "personal.files": {"icon":"","label":"Files","action":"uwsm-app -- nautilus ~/Documents"},
  //
  // Only use provider when a provider_name function or command named "name"
  // returns JSON rows. Static submenus only need dotted ids.
  //
  // Example: replace the default About action by reusing the same id. Existing
  // fields are kept unless overridden.
  // "about": {"icon":"","label":"About","action":"omarchy-launch-or-focus-tui \"zsh -c 'fastfetch; read -k 1'\""},
  // Screen auto-rotate, for the accelerometer in this convertible. Omarchy has
  // no rotation of its own; the `orientation` plugin owns the sensor daemon and
  // keeps its state as the `autorotate-off` toggle, so this row behaves like the
  // stock toggles beside it -- and flipping the toggle is the whole action, since
  // the plugin's service watches it. `checked` marks auto-rotate as on -- the flag
  // names the disabled state, so the check is its absence.
  "trigger.toggle.auto-rotate": {"icon":"󰑵","label":"Auto-rotate","when":"test -d ~/.config/omarchy/plugins/orientation","checked":"! omarchy-toggle-enabled autorotate-off","action":"omarchy-toggle autorotate-off"},

  // Quick task capture, the walker spotlight's replacement. The row closes the
  // menu and the plugin's `ticktick-add-launch` immediately summons its own
  // overlay, so the prompt stays the one place text gets typed. Named by path,
  // like the auto-rotate row above: the script ships in the `ticktick` plugin,
  // so the path is also the availability test.
  "task": {"icon":"","label":"Add Task","description":"Quick-add a TickTick task","aliases":["todo","ticktick"],"when":"test -x ~/.config/omarchy/plugins/ticktick/ticktick-add-launch","action":"~/.config/omarchy/plugins/ticktick/ticktick-add-launch"},
}
