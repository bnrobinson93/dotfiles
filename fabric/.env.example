# Fabric AI CLI Configuration Template
# Copy this file to .env and customize for your machine
#
# IMPORTANT: .env is gitignored and NOT symlinked by stow
# API credentials are primarily set via shell environment variables (fish/zsh configs)
# which provide OS-conditional configuration (work gateway vs personal keys)

# Default model and vendor
DEFAULT_VENDOR=Anthropic
DEFAULT_MODEL=claude-haiku-4-5

# Pattern and strategy repositories (public, safe to commit)
PATTERNS_LOADER_GIT_REPO_URL=https://github.com/danielmiessler/fabric.git
PATTERNS_LOADER_GIT_REPO_PATTERNS_FOLDER=data/patterns
PROMPT_STRATEGIES_GIT_REPO_URL=https://github.com/danielmiessler/fabric.git
PROMPT_STRATEGIES_GIT_REPO_STRATEGIES_FOLDER=data/strategies

# API Configuration (OPTIONAL - shell env vars take precedence)
# These can be set here for machine-specific overrides
# Uncomment and customize if needed:
#
# ANTHROPIC_API_BASE_URL=https://api.anthropic.com/
# ANTHROPIC_API_KEY=your-key-here
# OPENAI_API_BASE_URL=https://api.openai.com/v1
# OPENAI_API_KEY=your-key-here
